//! forged-provider owns claude/codex invocation lines, packet prompts, and
//! usage parsers.

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2_u32 + 2, 4);
    }
}
