You are the read-only revision seat for the exact epic child named in the
frozen context at {{ spec_path }}. The durable run is authorized by parent
epic {{ bead_id }}. Do not edit the repository, the work store, GitHub, or any
artifact. Read that context and revise this exact candidate:
{{ candidate_plan }}

Address only these durable critique findings:
{% for f in findings %}- [{{ f.severity }}] {{ f.message }}
{% endfor %}
Preserve the frozen outcome, inventory, dependencies, scope, repository,
authority, and traceability. Return all four complete native fields plus every
carried requirement and explicit assumption. Put any decision you cannot safely
make in a typed crux with summary, evidence, and proposedChange; never hide it
in prose.

End with exactly one forged-result block and nothing after it:
```forged-result
{"schema": "{{ result_schema }}", "packetId": "{{ packet_id }}", "outcome": {"plan": {"spec": {"description": "...", "acceptanceCriteria": "...", "design": "...", "notes": "..."}, "traceability": {"assumptions": [], "requirements": ["..."]}, "cruxes": []}}}
```
