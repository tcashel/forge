//! forged-ledger owns state.db DDL, the idempotent operation store, and the
//! attempt saga.

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2_u32 + 2, 4);
    }
}
