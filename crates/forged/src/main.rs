//! forged owns the clap CLI and the rmcp MCP server over one core.

fn main() {
    println!("forged: under construction");
}

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2_u32 + 2, 4);
    }
}
