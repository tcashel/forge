// Sentinel value used in the new-spec modal's repo <select> to mean
// "I'll type a custom path below." Lives here so any non-modal caller
// can still import it without dragging in the (deleted) modal.js.
export const CUSTOM_REPO_VALUE = "__custom__";
