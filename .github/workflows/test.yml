name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  bun-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        id: setup-bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1"
        continue-on-error: true

      - name: Install deps
        id: install
        if: steps.setup-bun.outcome == 'success'
        run: bun install --frozen-lockfile 2>&1 | tee /tmp/install.log
        continue-on-error: true

      - name: Run tests
        id: test
        if: steps.install.outcome == 'success'
        run: bun run test 2>&1 | tee /tmp/test.log
        continue-on-error: true

      - name: Post diagnostics
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          echo "### CI Test Diagnostics" > /tmp/comment.md
          echo "" >> /tmp/comment.md
          echo "**setup-bun:** ${{ steps.setup-bun.outcome }}" >> /tmp/comment.md
          echo "**install:** ${{ steps.install.outcome }}" >> /tmp/comment.md
          echo "**test:** ${{ steps.test.outcome }}" >> /tmp/comment.md
          echo "" >> /tmp/comment.md
          bun --version >> /tmp/comment.md 2>&1 || echo "bun not available" >> /tmp/comment.md
          echo "" >> /tmp/comment.md
          if [ -f /tmp/install.log ]; then
            echo '<details><summary>Install output</summary>' >> /tmp/comment.md
            echo '' >> /tmp/comment.md
            echo '```' >> /tmp/comment.md
            tail -100 /tmp/install.log >> /tmp/comment.md
            echo '```' >> /tmp/comment.md
            echo '</details>' >> /tmp/comment.md
          fi
          if [ -f /tmp/test.log ]; then
            echo '<details><summary>Test output</summary>' >> /tmp/comment.md
            echo '' >> /tmp/comment.md
            echo '```' >> /tmp/comment.md
            tail -100 /tmp/test.log >> /tmp/comment.md
            echo '```' >> /tmp/comment.md
            echo '</details>' >> /tmp/comment.md
          fi
          if [ -n "$PR_NUMBER" ]; then
            gh pr comment "$PR_NUMBER" --body-file /tmp/comment.md || echo "Failed to post comment"
          fi

      - name: Fail if any step failed
        if: always() && (steps.setup-bun.outcome == 'failure' || steps.install.outcome == 'failure' || steps.test.outcome == 'failure')
        run: exit 1
